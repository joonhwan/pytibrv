[build-system]
requires = ["setuptools", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "pytibrv"
version = "0.1.0"
description = "TIBRV Python bindings"
authors = [
    { name = "Arien Chen", email = "arienchen@example.com" }
]
license = "MIT"
dependencies = []